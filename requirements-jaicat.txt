# ---- Core utils/UI ----
pillow==10.4.0
imagehash==4.3.1
tqdm==4.66.4
scikit-learn==1.5.1
pandas==2.2.2

# ---- CV / Vision ----
opencv-python==4.9.0.80
opencv-contrib-python==4.9.0.80
onnxruntime==1.18.0
ultralytics==8.2.103

# ---- Face recognition (Windows + Py3.12) ----
cmake==3.29.2
dlib-bin==19.24.6
face-recognition==1.3.0

# ---- CLIP route (prefer open-clip; lighter than full HF)
open-clip-torch==2.24.0
ftfy==6.2.0
regex==2024.5.15

# ---- Optional HF bits (kept minimal, no TF)
transformers==4.41.2
accelerate==0.30.1
safetensors==0.4.2

# ---- NSFW / Safety (tag-only)
nudenet==3.4.1
scikit-image==0.24.0

# ---- Misc your code references ----
python-dateutil==2.9.0
